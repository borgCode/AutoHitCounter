<UserControl x:Class="AutoHitCounter.Views.Controls.HotkeysTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <DataTemplate x:Key="HotkeyItemTemplate">
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="{Binding DisplayName}"
                           Width="130"
                           VerticalAlignment="Center" />
                <TextBox Text="{Binding HotkeyText}"

                         Style="{StaticResource HotkeyTextBoxStyle}"
                         Tag="{Binding ActionId}"
                         GotFocus="HotkeyTextBox_GotFocus"
                         LostFocus="HotkeyTextBox_LostFocus"
                         KeyDown="HotkeyTextBox_KeyDown" />
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <Border BorderThickness="1" BorderBrush="#333" CornerRadius="3" Margin="5">
        <StackPanel Margin="10">
            <TextBlock Text="Options" FontWeight="Bold" Margin="0,0,0,10" />

            <StackPanel Margin="0,5,0,10">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <CheckBox Content="Enable Hotkeys"
                              IsChecked="{Binding IsEnableHotkeysEnabled}"
                              Margin="0,0,10,0" />
                    <CheckBox Content="Enable Global Hotkeys"
                              IsEnabled="{Binding IsEnableHotkeysEnabled}"
                              IsChecked="{Binding IsGlobalHotkeysEnabled}"
                              ToolTip="When disabled, hotkeys only work while the game window is focused. Custom games always have global hotkeys enabled."
                              Margin="0,0,0,0" />
                    <CheckBox Content="Block Game Actions"
                              IsEnabled="{Binding IsEnableHotkeysEnabled}"
                              IsChecked="{Binding IsBlockGameHotkeysEnabled}"
                              Margin="10,0,0,0" />
                    
                </StackPanel>
                <TextBlock Text="Hotkeys are always active when a custom game is selected."
                           FontSize="10" FontStyle="Italic" Foreground="#777"
                           Margin="0,2,0,0" />
            </StackPanel>

            <ScrollViewer MaxHeight="350" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding HotkeyBindings}"
                              Padding="5"
                              ItemTemplate="{StaticResource HotkeyItemTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="2" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
    </Border>
</UserControl>